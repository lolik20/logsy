// Карта сервиса для языковых моделей (конвенция llms.txt).
//
// Агент, которому дали ссылку на Logsy, читает этот файл и сразу понимает: что это за
// сервис, какие есть методы, как авторизоваться и куда идти за подробностями. Файл
// текстовый и короткий — на него не тратится контекст, в отличие от HTML-страниц.

import { API_ENDPOINTS, appUrl } from "@/lib/api-docs";

export const dynamic = "force-dynamic";

export async function GET() {
  const base = appUrl();
  const methods = API_ENDPOINTS.map(
    (e) => `- [${e.method} ${e.path}](${base}/docs/api/${e.slug}): ${e.summary}`,
  ).join("\n");

  const body = `# Logsy

> Российский сервис мониторинга и логирования сайтов: проверки доступности раз в минуту,
> сбор ошибок фронтенда со стектрейсом, упавшие и медленные сетевые запросы, запись
> пользовательских сессий, контроль SSL-сертификата и срока регистрации домена.
> Данные хранятся в РФ, оплата в рублях.

Домен сервиса: ${base}

## Что можно получить через API

Публичное HTTP-API проекта отдаёт то же, что видно в панели: сессии посетителей и все
события внутри них — ошибки JavaScript, HTTP-ошибки и медленные запросы с телом и
заголовками, переходы, клики, ввод и обращения из формы «Сообщить об ошибке».
Типичная задача агента: найти сессии с ошибками за период и развернуть проблемную,
чтобы починить причину, а не симптом.

## Авторизация

Ключ проекта выдаётся в панели на вкладке «API». Передаётся заголовком:

    Authorization: Bearer <ключ>
    X-Api-Key: <ключ>

Ключ даёт доступ ко всем логам сайта — держите его в переменной окружения
(LOGSY_API_KEY) и не отдавайте в браузер.

## Методы

${methods}

## Машиночитаемые описания

- [OpenAPI 3.1](${base}/api/v1/openapi.json): полная спецификация методов
- [Документация API](${base}/docs/api): та же информация для человека
- [Подключение к ИИ-агентам](${base}/docs/for-agents): пошагово, с готовыми запросами

## Подключение сайта

Одна строка в <head> сайта включает сбор логов:

    <script src="${base}/api/logger/sdk" defer></script>

Проект определяется по домену, с которого пришёл запрос, поэтому ключ для сбора логов
не нужен — он нужен только для чтения данных снаружи.

## Ограничения

- Данные доступны в пределах срока хранения логов по тарифу проекта.
- Список сессий отдаёт максимум 200 записей за запрос, дальше — параметр offset.
- Чанки записи экрана через API не отдаются: приходит только признак наличия записи.
`;

  return new Response(body, {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, max-age=3600",
    },
  });
}
