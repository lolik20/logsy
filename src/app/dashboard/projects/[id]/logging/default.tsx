// Фон под модалкой при переходе на перехваченный маршрут с чужой страницы
// (например, из «Пользователи → Сессии»): рендерим обычный список сессий.
// searchParams сюда не передаются, поэтому список показывает сегодняшний день.
export { default } from "./page";
