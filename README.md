# Logsy — сервис мониторинга uptime

Мониторинг доступности сайтов и API для российского рынка. Пользователь
регистрируется, добавляет проекты (домены) и мониторы (URL + HTTP-метод +
периодичность), а фоновый воркер каждую минуту проверяет эндпоинты и шлёт
email-алерты при падении.

## Возможности

- **Лендинг** с описанием и кастомными тарифами: базовый бесплатный объём плюс
  доплата за расширение по ползункам (см. «Тарифы» ниже).
- **Авторизация** через Яндекс ID или по почте (пароль генерируется на бэкенде
  и отправляется письмом).
- **Проекты** → **мониторы**: URL для проверки, метод `GET/POST/PUT/DELETE`,
  периодичность `1m / 1h / 1d`, ожидаемый код ответа.
- **Планировщик** (node-cron) обходит БД каждую минуту и проверяет мониторы,
  которым подошёл срок.
- **Алерты на почту и в Telegram** при переходе монитора в статус DOWN (и при
  восстановлении). Каналы настраиваются в панели на вкладке **«Контакты»**.
  Telegram работает на **long polling** (без публичного webhook) и умеет ходить
  через прокси. Пользователь жмёт кнопку «Открыть бота», бот по команде `/start`
  присылает `chat id`, который пользователь вставляет в панели — создаётся
  контакт с типом `TELEGRAM`.
- **Контроль SSL-сертификата** домена проекта: тумблер в настройках проекта
  включает слежение за сроком действия сертификата. Письмо-предупреждение
  уходит за неделю, за 3 дня, за 1 день и за 1 час до истечения (и когда
  сертификат уже истёк). Каждое из оповещений отправляется один раз.
- **Контроль срока регистрации домена** проекта: тумблер в настройках проекта
  включает слежение за датой окончания регистрации домена (данные регистратора
  запрашиваются по **RDAP** — современной HTTP/JSON-замене WHOIS). Предупреждение
  на все контакты (email и Telegram) уходит за месяц, за 2 недели, за неделю, за
  3 дня и за 1 день до окончания (и когда регистрация уже истекла). Каждое
  оповещение отправляется один раз; при продлении домена пороги срабатывают
  заново.
- **Напоминание об окончании подписки**: за сутки до истечения текущего периода
  (пробного или оплаченного) на **все контакты** пользователя (email и Telegram)
  уходит предупреждение о том, что подписка скоро закончится и мониторинг будет
  остановлен. Отправляется один раз за период — при продлении срабатывает заново
  для нового срока.
- **История проверок** и uptime по каждому монитору.
- **Обратная форма ошибок** в сервисе логирования: тумблер на вкладке
  **«Подключение»** включает на сайте проекта плавающую кнопку «Сообщить об
  ошибке» (правый нижний угол на ПК и мобильных). Посетитель указывает свою
  **почту** (поле с валидацией формата) и кратко описывает проблему — сообщение
  приходит событием `USER_REPORT` в его сессию, подсвечивается в ленте событий и
  отмечается фиолетовой иконкой чата в карточке сессии на странице логов. Почта
  отправителя сохраняется в событии и показывается рядом с сообщением. О каждом
  сообщении владелец проекта получает уведомление на все свои контакты (email и
  Telegram) — со ссылкой на сессию пользователя в панели. Дополнительно по
  каждому сообщению **автоматически создаётся задача** в статусе «Создано» на
  вкладке «Задачи» проекта. Клиентский SDK узнаёт, включена ли форма, из
  `GET /api/logger/config` (проект определяется по `Origin`, как и приём логов).
- **Запись экрана сессий** (rrweb): тумблер «Запись экрана сессий» на вкладке
  **«Подключение»** включает на сайте проекта запись действий посетителя. Клиентский SDK
  подгружает self-hosted рекордер (`/logsy-rec.js` — vendored `rrweb`, без сторонних CDN),
  снимает полный DOM-снимок и инкрементальные изменения (мутации, движения мыши, клики,
  ввод, прокрутку, навигацию) и батчами шлёт их на `/api/logger/rec` (та же keyless-модель
  по `Origin`, что и приём логов). Запись привязывается к пользовательской сессии; на
  странице сессии по кнопке **«Смотреть запись»** она воспроизводится как видео с таймлайном
  (плеер `rrweb-player`, `/api/recordings/[sessionId]`). Приватность: значения полей ввода и
  пароли маскируются на клиенте до отправки, а элементы с атрибутом `data-logsy-mask`
  скрываются полностью. Записи хранятся тот же срок, что и логи (ретеншн по тарифу), и
  чистятся вместе с ними. По умолчанию запись выключена — включается осознанно, т.к. объёмна.
- **Задачи проекта** (вкладка **«Задачи»**) — доска-канбан с тремя статусами:
  **Создано → В работе → Выполнено**. Задачу можно создать вручную,
  отредактировать, удалить и перетащить между колонками (drag & drop). Задачи из
  обратной формы ошибок попадают в колонку «Создано» и помечены значком-источником;
  в карточке такой задачи видна почта отправителя и ссылка на сессию пользователя.
- **Переписка с пользователем по задаче** — если у задачи есть почта пользователя
  (обычно это задачи из обратной формы ошибок), в окне редактирования появляется
  лента переписки: первое обращение, входящие и исходящие сообщения. Владелец
  проекта пишет ответ прямо в карточке — он уходит **письмом** на почту обращения
  (`Reply-To` ставится на почту владельца, чтобы прямой ответ пользователя пришёл
  ему). Каждый ответ сохраняется в переписке как исходящее сообщение. Если тот же
  посетитель пишет из обратной формы ещё раз (в той же сессии, с той же почтой) и
  его задача ещё не в статусе «Выполнено», сообщение **дописывается в переписку
  существующей задачи**, а не заводит новую. На карточке задачи показывается
  счётчик сообщений переписки.

## Стек

Next.js 14 (App Router) · React · TypeScript · Prisma (PostgreSQL) ·
Auth.js (NextAuth v5) · nodemailer · node-cron · Tailwind CSS · zod ·
rrweb / rrweb-player (запись и воспроизведение сессий).

## Быстрый старт

```bash
cp .env.example .env      # укажите DATABASE_URL к вашему PostgreSQL
npm install
npm run db:push           # создаст таблицы в PostgreSQL
npm run dev               # http://localhost:3000
```

`DATABASE_URL` имеет вид
`postgresql://user:password@host:5432/logsy?schema=public`.

Регистрация: на `/register` укажите email. Без настроенного SMTP пароль будет
**выведен в консоль сервера** — скопируйте его и войдите на `/login`.

### Восстановление пароля

Забыли пароль — на `/login` есть ссылка «Забыли пароль?» (страница
`/forgot-password`). Укажите email → на почту придёт ссылка на страницу
`/reset-password?token=…` (действует 1 час), где в **одно поле** задаётся новый
пароль. Токен одноразовый и хранится в таблице `VerificationToken` (по аналогии
с быстрым входом). Без настроенного SMTP ссылка **выводится в консоль сервера**.

### Запуск проверок

Проверки запускаются **автоматически внутри сервера Next.js** через хук
инструментации (`src/instrumentation.ts`). Как только сервер поднят
(`npm run dev` или `npm run build && npm start`), планировщик (node-cron)
каждую минуту обходит активные мониторы, у которых подошёл срок по их интервалу
(`1m/1h/1d`), пишет результаты и отправляет алерты. Отдельную команду запускать
не нужно.

> Важно: инструментация работает в долгоживущем Node-процессе (`next start` на
> своём сервере/VPS). На serverless-хостинге (напр. Vercel) фоновые задачи не
> живут между запросами — там используйте внешний cron на эндпоинт ниже.

Дополнительные способы (не обязательны):

```bash
# отдельный процесс-воркер (альтернатива инструментации)
npm run worker

# внешний cron дёргает эндпоинт
curl -X POST "http://localhost:3000/api/cron/run?token=$CRON_SECRET"
```

## Переменные окружения

См. `.env.example`. Ключевые:

| Переменная | Назначение |
|---|---|
| `DATABASE_URL` | Строка подключения к PostgreSQL |
| `AUTH_SECRET` | Секрет Auth.js (`openssl rand -base64 32`) |
| `AUTH_YANDEX_ID` / `AUTH_YANDEX_SECRET` | Yandex ID OAuth (иначе кнопка скрыта) |
| `ADMIN_EMAILS` | Email-адреса администраторов через запятую (см. ниже) |

### Yandex ID OAuth

1. Создайте приложение на <https://oauth.yandex.ru/>.
2. В поле **Redirect URI** (Callback URL) укажите:

   ```
   ${NEXTAUTH_URL}/api/auth/callback/yandex
   ```

   Локально это `http://localhost:3000/api/auth/callback/yandex`,
   в проде — `https://ваш-домен/api/auth/callback/yandex`.
3. Выдайте права: «Доступ к адресу электронной почты» и «Доступ к логину, имени и фамилии».
4. Скопируйте `ID` и `Client secret` приложения в `AUTH_YANDEX_ID` и `AUTH_YANDEX_SECRET`.

Колбэк-эндпоинт обрабатывается автоматически catch-all роутом
`src/app/api/auth/[...nextauth]/route.ts` — отдельный обработчик не нужен.
| `SMTP_*` | SMTP для писем (иначе письма пишутся в консоль) |
| `CRON_SECRET` | Токен для `POST /api/cron/run` |
| `TELEGRAM_BOT_TOKEN` | Токен бота из @BotFather (иначе Telegram-алерты пишутся в консоль) |
| `TELEGRAM_PROXY_URL` | Необязательный HTTP(S)-прокси до `api.telegram.org` (напр. `http://user:pass@host:3128`) |
| `TELEGRAM_BOT_USERNAME` | Необязательно: username бота без `@`. Если не задан, берётся автоматически из `getMe` |

### Алерты в Telegram

Бот работает на **long polling** — долгоживущий процесс (сервер Next.js через
`src/instrumentation.ts` или отдельный `npm run worker`) сам опрашивает Telegram
методом `getUpdates`. Публичный webhook и открытый наружу адрес не нужны, а если
Telegram недоступен напрямую — запросы идут через `TELEGRAM_PROXY_URL`.

1. Создайте бота у [@BotFather](https://t.me/BotFather), получите **токен** и
   положите его в `TELEGRAM_BOT_TOKEN`. При необходимости укажите
   `TELEGRAM_PROXY_URL`. Поднимите долгоживущий процесс (`npm start` или
   `npm run worker`) — в логах появится «Telegram long polling запущен».
2. Пользователь на вкладке **«Контакты»** жмёт «Открыть бота», нажимает
   «Запустить» (`/start`) — бот присылает `chat id`. Пользователь вставляет его
   в поле и сохраняет: в БД появляется `Contact` с типом `TELEGRAM` и значением
   `chat id`, и туда начинают приходить алерты о падении мониторов и SSL.

> Важно: `getUpdates` должен опрашивать только один процесс. Если запущены и
> `next start`, и `npm run worker` одновременно, Telegram вернёт 409 Conflict —
> оставьте что-то одно (как и для планировщика проверок).

## Роли и админ-панель

У пользователя есть роль (`role` в модели `User`): `USER` (по умолчанию) или
`ADMIN`. Роль хранится в БД, поэтому после обновления схемы выполните
`npm run db:push`, чтобы добавить колонку.

Чтобы назначить администратора, перечислите его email в переменной
`ADMIN_EMAILS` (через запятую) — при первом входе такому пользователю
автоматически проставляется роль `ADMIN`:

```
ADMIN_EMAILS="admin@example.com, ops@example.com"
```

Что получает администратор:

- вкладку **«Пользователи»** в боковом меню — список всех пользователей с
  краткой информацией (email, имя, роль, статус подписки, число проектов и
  мониторов, дата регистрации);
- **выдачу тарифа конкретному проекту**: открыв любой проект на вкладке
  **«Тариф»**, администратор может **выдать проекту активный тариф до выбранной
  даты** с нужными лимитами (суточная квота сессий и срок хранения логов),
  обновить их или **снять** тариф. Выданный тариф активирует мониторинг и
  логирование проекта до конца указанного дня; «Снять» возвращает проект на
  бесплатный тариф и сбрасывает лимиты к бесплатным;
- вкладку **«Проверки скорости»** (после «Пользователи») — список прогонов
  публичного инструмента проверки скорости сайта: домен, время прогона и
  скорость (готовность DOM). Каждый запуск на `/speed-test` сохраняется в
  таблицу `SpeedCheck`;
- на вкладке **«Мониторинг»** — проекты всех пользователей (сгруппированы по
  владельцу) с возможностью открыть любой проект и посмотреть каждый монитор.
  Управление (добавление/редактирование/удаление) остаётся только у владельца.

## Переход на PostgreSQL

В `prisma/schema.prisma` смените `provider = "sqlite"` на `"postgresql"`,
укажите `DATABASE_URL`, затем `npm run db:push`.

## Оплата через Т-Кассу (T-Bank эквайринг)

Биллинг (за проект) подключён к **Т-Кассе** методом `Init`
(<https://developer.tbank.ru/eacq/api/init>). Тариф **кастомный** и настраивается
ползунками: базовый объём — **300 сессий в сутки** и хранение логов **12 часов** —
бесплатен навсегда, а сверх него начисляется помесячная доплата: **2 ₽/мес** за
каждую суточную сессию сверх 300 и **10 ₽/мес** за каждый час хранения сверх 12
(константы в `src/lib/pricing.ts`). Доступны три периода оплаты: **1 месяц** (без
скидки), **3 месяца** (−10%) и **1 год** (−20%). Оплаченный период **добавляется к
текущей дате окончания** тарифа (продление): если тариф ещё активен, месяцы
прибавляются к остатку, иначе отсчёт идёт от даты оплаты. После окончания
оплаченного периода проект возвращается к бесплатным лимитам.

1. На вкладке «Тариф» пользователь ползунками задаёт суточную квоту сессий и срок
   хранения логов и жмёт «Оплатить» → `POST /api/billing/pay` создаёт платёж и
   возвращает `PaymentURL`.
2. В запрос кладётся объект **Receipt** (чек по 54-ФЗ): система
   налогообложения **УСН** (`Taxation`) и **email пользователя** (`Email`),
   на который Т-Касса отправит чек.
3. Пользователь оплачивает на странице Т-Кассы. После оплаты Т-Касса шлёт
   нотификацию на `POST /api/billing/tbank/notification` — подпись `Token`
   проверяется, и при статусе `CONFIRMED` подписка активируется/продлевается
   на месяц.

Ключи и подпись (`Token`) считаются по правилам Т-Кассы: SHA-256 от
отсортированных значений простых параметров запроса плюс `Password`
(см. `src/lib/tbank.ts`).

### Переменные окружения Т-Кассы

| Переменная | Назначение |
|---|---|
| `TBANK_TERMINAL_KEY` | Ключ терминала из личного кабинета Т-Кассы |
| `TBANK_PASSWORD` | Пароль терминала (для подписи запросов) |
| `TBANK_TAXATION` | Система налогообложения чека: `usn_income` (по умолчанию), `usn_income_outcome`, `osn`, `envd`, `esn`, `patent` |
| `TBANK_API_URL` | Базовый URL API (по умолчанию `https://securepay.tinkoff.ru/v2`) |
| `APP_URL` | Базовый адрес сайта — из него строятся Success/Fail/Notification URL |

> В личном кабинете Т-Кассы для терминала должен быть указан адрес
> нотификаций `${APP_URL}/api/billing/tbank/notification`.

## Заметки

- Бесплатный тариф (до 300 сессий в сутки, хранение логов 12 часов) — состояние
  проекта по умолчанию. Действует без ограничения по времени; расширение лимитов
  (больше сессий и/или дольше хранение) настраивается ползунками на вкладке
  «Тариф» и оплачивается помесячно.
